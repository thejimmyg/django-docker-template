from .base_07_geo import *

# Don't do this.
# AUTH_USER_MODEL = 'mysite.User'
# 
# 
# # So that mysite.models gets registered:
# # Make sure it is first so the migrations work:
# if 'myactualsite' in INSTALLED_APPS:
#     INSTALLED_APPS.pop(INSTALLED_APPS.index('myactualsite'))
# INSTALLED_APPS.insert(0, 'myactualsite')
